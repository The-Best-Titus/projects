#ifndef ARRAY_STATS_H
#define ARRAY_STATS_H

// Find the maximum value in an integer array
int findMax(int arr[], int size);

// Calculate the average of values in an integer array
double calculateAverage(int arr[], int size);

// Count how many values in the array are above a given threshold
int countAbove(int arr[], int size, int threshold);

#endif
